local DeploymentManifest = table.freeze({
    schemaVersion = 1,
    releaseChannel = "private-staging",
    releaseId = "starblox-private-step9-joinfix-v1",
    productionActivationAllowed = false,
})

return DeploymentManifest
