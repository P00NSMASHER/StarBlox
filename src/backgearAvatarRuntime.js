const BACKGEAR_ART=Object.freeze({'backgear-1':'/assets/catalog/backgear-1.svg','backgear-2':'/assets/catalog/backgear-2.svg'});
function equippedBackgearId(){if(typeof localStorage==='undefined')return '';for(const key of ['starblox-save-v2','abvm-brightside-floot-v2','abvm-brightside-floot-v1']){try{const raw=localStorage.getItem(key);if(!raw)continue;const save=JSON.parse(raw);if(save?.equipped?.back)return save.equipped.back;}catch{}}return '';}
function syncBackgearArt(){if(typeof document==='undefined')return;const node=document.querySelector('.backGear');if(!node)return;const itemId=equippedBackgearId();const assetPath=BACKGEAR_ART[itemId];if(assetPath){node.dataset.portableBackgearId=itemId;node.classList.add('portableBackGear');node.textContent='';node.style.setProperty('--backgear-art',`url("${assetPath}")`);node.setAttribute('aria-label','Equipped back gear');return;}if(node.dataset.portableBackgearId){delete node.dataset.portableBackgearId;node.classList.remove('portableBackGear');node.style.removeProperty('--backgear-art');if(!node.textContent)node.textContent='✦';}}
if(typeof document!=='undefined'){const kick=()=>window.setTimeout(syncBackgearArt,0);document.addEventListener('click',kick,true);window.addEventListener('storage',kick);const observer=new MutationObserver(syncBackgearArt);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',syncBackgearArt,{once:true});else syncBackgearArt();}
export {BACKGEAR_ART,syncBackgearArt};
